# Local development defaults.
# The app runs without copying this file; use these values only if you need to override defaults.

# API server
PORT=8080
LOG_LEVEL=info

# Frontend/Vite dev server
BASE_PATH=/
API_PROXY_TARGET=http://localhost:8080
