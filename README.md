# LogoMatch-AI-Recognize-Logo
LogoMatch AI is a few-shot logo recognition system using 5 reference images per company to create a single prototype vector (average of normalized embeddings). New logos are matched via cosine similarity with confidence thresholds—no retraining for new companies. It includes OCR assist, evaluation harness, human review queue, and active learning.
